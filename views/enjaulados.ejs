<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Enjaulados</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/animalitos.css">
  <link rel="stylesheet" data-purpose="Layout StyleSheet" title="Web Awesome"
    href="/css/app-wa-4605c815f1874757bc9ac33aa114fb0f.css?vsn=d">
  <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.7.2/css/all.css">
</head>

<body class="bg-gray-900 text-gray-100 p-8 transition-mode">
  <div class="container mx-auto max-w-7xl">
    <div class="text-right mb-6 flex justify-between items-center">
      <a href="/"
                class="px-4 py-2 bg-zinc-700 text-white rounded-md shadow-md hover:bg-zinc-600 transition duration-300 flex items-center gap-2 w-max">
                <li class="fas fa-home"></li> Inicio
            </a>
      <img src="images/logo_lotto.png" alt="Logo_lotto" class="h-24 w-24 max-content">
    </div>

    <h1 class="text-5xl font-extrabold text-white text-center transition-colors">
      Animalitos Enjaulados
    </h1>
    <div class="flex justify-center p-4 mr-2">
      <a href="/estadisticas"
          class="w-max px-4 py-2 bg-red-600 text-white rounded-md shadow-md hover:bg-red-700 transition duration-300">
          <i class="fas fa-filter mr-1"></i>Más salidos
      </a>
  </div>

    <% if (animalDiasSinSalir.length > 0) { %>
    <div class="overflow-x-auto bg-gray-800 rounded-lg shadow-xl p-6">
      <table class="min-w-full table-auto text-center">
        <thead>
          <tr class="bg-gray-700 text-gray-300">
            <th class="py-3 px-4"><i class="fa fa-image mr-1"></i>Imagen</th>
            <th class="py-3 px-4"><i class="fa fa-paw mr-1"></i>Animal</th>
            <th class="py-3 px-4"><i class="fa fa-calendar-days mr-1"></i>Última fecha</th>
            <th class="py-3 px-4"><i class="fa fa-sun-haze"></i> Días sin salir</th>
          </tr>
        </thead>
        <tbody class="text-gray-100">
          <% animalDiasSinSalir.forEach(animal => { %>
            <tr>
              <td class="py-3 px-4 flex justify-center">
                <img src="<%= animal.imagenURL %>" alt="<%= animal.animal %>" class="w-28 h-28 object-contain">
              </td>
              <td class="py-3 px-4 font-semibold ml-2"><%= animal.animal %> - <%= animal.numeroAnimal %></td>
              <td class="py-3 px-4 ml-2"><%= animal.ultimaFecha %></td>
              <td class="py-3 px-4 ml-2"><%= animal.diasSinSalir %> días</td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } else { %>
      <p class="text-center text-xl text-gray-400 mt-12">No hay animales sin salir por ahora.</p>
    <% } %>

  </div>

  <script src="/javascripts/interactionEjs.js"></script>
</body>

</html>
